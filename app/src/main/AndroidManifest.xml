<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matronina.myproject">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.alerts.AlertsActivity"
            android:label="@string/title_alerts"/>
        <activity
            android:name=".ui.health.HealthActivity"
            android:label="@string/title_health" />
        <activity
            android:name=".ui.energy.EnergyActivity"
            android:label="@string/title_energy"/>
        <activity
            android:name=".ui.protection.ProtectionActivity"
            android:label="@string/title_protection" />
        <activity
            android:name=".ui.admin.AdminActivity"
            android:label="@string/title_admin" />
    </application>

</manifest>